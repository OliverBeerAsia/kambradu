/**
 * The language a learner is currently working in.
 *
 * This milestone is Kristang only, so the tag is a constant. It lives here, and
 * not as a literal inside thirteen components, because the next language should
 * be a one-line change rather than a search and replace through the interface.
 *
 * `mcm` is the ISO 639-3 code for Malacca Creole Portuguese.
 */
export const LANGUAGE_TAG = "mcm";
